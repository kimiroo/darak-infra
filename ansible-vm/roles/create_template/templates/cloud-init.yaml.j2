#cloud-config
package_update: true
package_upgrade: true

packages:
  - kernel-modules-extra

power_state:
  delay: "now"
  mode: reboot
  message: "Rebooting to apply new kernel modules and packages"
  condition: true